export enum ButtonType {
  primary = "primary-button",
  secondary = "secondary-button",
  styled="styled-button"
}

export enum IntervalPlace {
  negative = "red",
  positive = "green",
  neutral= "black",
}
